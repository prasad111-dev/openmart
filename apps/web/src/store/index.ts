// Backwards-compatible barrel — re-exports from new store locations
export { useAuthStore } from '@/stores/auth.store'
export { useCartStore } from '@/stores/cart.store'
export type { CartItem, Cart } from '@/stores/cart.store'
